# 给队友的分享指南

> 3 种方案, 按推荐程度排序. 选最适合你们场景的.

---

## 方案 1: 局域网共享 (最推荐, 比赛当天用)

**原理**: 一台电脑当"主机"跑后端, 其他队友手机/电脑连同一 WiFi, 打开主机的 IP 地址即可看到同一个系统.

### 步骤

**A. 主机方 (你做):**
1. 双击 `启动.bat`, 让后端在跑
2. 双击 `查看IP.bat`, 记住显示的 IP 地址 (例如 `192.168.1.100`)
3. 把你电脑连上比赛现场 WiFi (或者手机开热点)

**B. 队友方:**
- 队友手机/电脑连**同一 WiFi**
- 浏览器打开: `http://你的IP:3000/student.html` (或 driver.html)
- 例如: `http://192.168.1.100:3000/student.html`

### 优点
- 不需要任何注册/账号
- 比赛现场断网也能用 (手机开热点)
- 实时同步: 队友上报, 你那边立刻看到

### 限制
- 必须在同一 WiFi/局域网
- 主机关机 = 全员断线

---

## 方案 2: 各自电脑运行 (适合赛前练习)

**原理**: 每个队友各自装 Node.js, 各自跑一份, **互不影响**.

### 步骤

1. **把项目文件发给队友**
   - 推荐: 用 `打包发给队友.bat` (一键打包, 排除 node_modules)
   - 然后微信/钉钉/QQ 发压缩包给他们
2. **队友解压缩, 双击 `启动.bat` 即可** (需要先装 Node.js, 一次性的)

### 优点
- 不依赖网络, 各自独立
- 每人随时能练

### 限制
- 每人看到的**状态是独立的** (你上报的, 队友那边看不到)
- 适合各自练操作, 不适合联调

---

## 方案 3: 公网部署 (适合远程分享/不需要 WiFi)

**原理**: 把你电脑的 3000 端口暴露到公网, 队友用浏览器随时访问.

### 工具: ngrok (免费)

1. 去 https://ngrok.com 注册, 下载客户端
2. 解压, 在 ngrok.exe 所在目录打开 cmd, 输: `ngrok config add-authtoken 你的token`
3. 启动后端 (双击 `启动.bat`)
4. 另开 cmd, 输: `ngrok http 3000`
5. 会显示一行 `Forwarding https://xxxx.ngrok-free.app -> http://localhost:3000`
6. 把这个 `https://xxxx.ngrok-free.app` 链接发给队友, 队友浏览器打开就 + `/student.html` 或 `/driver.html`

### 优点
- 任何地方任何网络都能访问
- 一个人跑, 全员同步

### 限制
- 你的电脑必须一直开着
- 免费版每月 1GB 流量 (演示够用)
- 注册略麻烦 (但只要一次)

---

## 比赛当天建议

| 时段 | 方案 |
|------|------|
| **赛前一周各自练** | 方案 2 (各自跑) |
| **赛前联调** | 方案 1 (你当主机, 队友手机连) |
| **比赛当天现场** | 方案 1 (你当主机, 现场 WiFi 或手机热点) |
| **备份方案 (断网)** | 方案 2 录演示视频备用 |

---

## 文件清单 (给队友打包时)

```
campus-bus/
├── 启动.bat              ← 必备
├── 查看IP.bat            ← 局域网时用
├── README.md             ← 给队友看的说明
├── 分享指南.md            ← 本文件
├── package.json          ← 必备
├── server.js             ← 必备
└── public/               ← 必备 (整个文件夹)
    ├── student.html
    ├── driver.html
    ├── css/
    └── js/
```

**不要打包**: `node_modules/` (队友自己装), `server.log` (调试日志)
